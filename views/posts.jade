extends layout

block content
    .posts
        each val in posts
            .post-container
                .title=val.title
                .date=val.date
                //-.post-marker
                .post
                    !{val.post}